<!DOCTYPE html>
<html>
<head>
    <title>Users List</title>
</head>
<body>
    <h1>Users</h1>
    <ul>
        {% for user in users %}
            <li>{{ user.id }} {{ user.name }} {{ user.surname }} {{ user.email }}</li>
        {% endfor %}
    </ul>
</body>
</html>
