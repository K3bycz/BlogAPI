<!DOCTYPE html>
<html>
<head>
    <title>Users List</title>
</head>
<body>
    <h1>Users</h1>
    <ul>
        {% for user in users %}
            <li>{{ user.id }} {{ user.name }} {{ user.surname }} {{ user.email }}</li>
        {% endfor %}
    </ul>
    <div style="text-align:center; margin:20px">
        <button style="padding:5px; background-color:lightgrey"><a href="/logout" style="color:black"> Wyloguj się </a></button>
    </div>
</body>
</html>
